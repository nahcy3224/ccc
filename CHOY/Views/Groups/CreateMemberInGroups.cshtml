@model CHOY.Models.GroupMember

@{
    ViewBag.Title = "新增群組成員";
}

    <h2>新增群組成員</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
<div class="form-horizontal">
    <h4>GroupMember</h4>
    <hr />

    <div class="form-group">
        <label for="Id" class="col-md-2">新增群組成員</label>
        <div class="col-md-10">
            <input id="Id" name="Id" type="text" class="form-control" />
        </div>
    </div>
    @*@Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @Html.HiddenFor(model => model.GroupID)

    @Html.HiddenFor(model => model.MemberIDOwner)

    @Html.DisplayFor(model => model.MemberIDInGroup)*@

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn btn-default" />
        </div>
    </div>
</div>
}

<div>
    @Html.ActionLink("Back to List", "Index","Groups")
</div>
